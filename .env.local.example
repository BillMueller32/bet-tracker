NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Used for the "add bet from screenshot" feature (reads bet slip
# screenshots with Claude's vision model). Get a key from
# https://console.anthropic.com — keep this secret, no NEXT_PUBLIC_ prefix.
ANTHROPIC_API_KEY=sk-ant-...
